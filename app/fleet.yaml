defaultNamespace: app-1
namespaceLabels:
  app: app1
namespaceAnnotations:
  app: app2
name: my-app
kustomize:
  dir: dev
diff:
  comparePatches:
  - apiVersion: apps/v1
    kind: Deployment
    name: frontend-deployment
    namespace: app1
    operations:
    - {"op":"ignore", "path":"/spec/replicas"}
